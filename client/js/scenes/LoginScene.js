 export class LoginScene extends Phaser.Scene {
 constructor() {
 super({ key: 'LoginScene' });
 }
 create() {
 // This scene is handled by HTML UI
// This scene is handled by HTML UI
 // Kept for potential future use
 }
 }